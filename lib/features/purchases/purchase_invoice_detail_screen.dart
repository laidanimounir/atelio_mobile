import 'package:flutter/material.dart';
class PurchaseInvoiceDetailScreen extends StatelessWidget { const PurchaseInvoiceDetailScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Purchase Detail')), body: const Center(child: Text('Purchase detail'))); }
