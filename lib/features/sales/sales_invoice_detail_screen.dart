import 'package:flutter/material.dart';
class SalesInvoiceDetailScreen extends StatelessWidget { const SalesInvoiceDetailScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Invoice Detail')), body: const Center(child: Text('Invoice detail'))); }
