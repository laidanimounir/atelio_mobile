name: atelio_mobile
description: Mobile companion app for ProManSystem - ATELIO
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.3
  go_router: ^14.8.1
  flutter_riverpod: ^2.6.1
  intl: ^0.19.0
  shimmer: ^3.0.0
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets: []
